/**
 * Função de inicialização do Agente Júlia.
 * No futuro, conterá a lógica para carregar a persona,
 * conectar-se a serviços, etc.
 */
function init() {
  console.log("Agente Júlia inicializado... (lógica futura aqui)");
}

// Exporta as funções para que possam ser usadas em outros arquivos (como o server.js)
module.exports = { init };